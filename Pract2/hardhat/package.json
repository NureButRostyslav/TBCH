{
  "name": "hardhat-local-project",
  "version": "1.0.0",
  "description": "Local Hardhat project for private blockchain",
  "private": true,
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "node scripts/deploy.js",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "^2.22.1",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "ethers": "^6.14.0",
    "dotenv": "^16.0.0"
  }
}